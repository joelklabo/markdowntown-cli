# Gemini Instructions

- Follow the repo workflow in `AGENTS.md`.
- Prefer `rg` for search and keep changes small and deterministic.
- Run `make lint` and `make test` before finishing work.
- CI must be green; investigate and fix failures before finishing.
