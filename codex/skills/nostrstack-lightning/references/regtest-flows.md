# Regtest flows (summary)

## Regtest payments
- Enable `ENABLE_REGTEST_PAY=true` in API.
- Enable `VITE_ENABLE_REGTEST_PAY=true` in gallery.

## Zaps / LNURL
- Confirm LNURLp and zap flows via gallery + regtest stack.
- Use regtest payer to settle invoices if needed.
