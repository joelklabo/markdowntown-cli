# Event reference resolution (summary)

## Reference sources
- Tag-based references (NIP-10 style)
- Inline `nostr:` mentions (NIP-27)

## UI expectations
- Inline `nostr:` mentions link to `/nostr/<id>`.
- Present referenced profiles/events with cached API-backed data when available.
