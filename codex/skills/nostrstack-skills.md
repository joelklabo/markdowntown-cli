# Nostrstack Codex skills catalog

For the full cross-project inventory, see `skills/skills-catalog.md`.

- nostrstack-dev-workflow: start local dev, run logs, MCP DevTools checks, QA fallback
- nostrstack-api: Fastify/Prisma API changes in apps/api
- nostrstack-nostr: Nostr IDs, event landing behavior, NIP-05 resolution
- nostrstack-embed-ui: embed widgets, tokens, gallery UI
- nostrstack-lightning: LNbits/regtest flows, Lightning ops/observability

Trigger examples:
- "Start the nostrstack dev stack and tail logs"
- "Update a Fastify route in apps/api and add tests"
- "Fix NIP-05 lookup behavior for nostr identifiers"
- "Change embed button styles to use design tokens"
- "Debug LNbits webhook handling in staging"
