# Example: `AGENTS.override.md`

This file can be used to override or extend the default agent instructions for a specific subtree.

## Overrides
- Prefer snapshot tests only when the UI is stable.
- If you touch routing, add a focused page test.
