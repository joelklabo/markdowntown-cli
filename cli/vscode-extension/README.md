# Markdowntown VS Code Extension

Language Server Protocol client for markdowntown.

## Features

- AI configuration file diagnostics.
- Quick fixes for common issues.
- Hover documentation for tool IDs.
- Document symbols for config blocks.

## Requirements

Requires the `markdowntown` CLI binary to be installed and in your PATH, or configured via `markdowntown.serverPath`.
